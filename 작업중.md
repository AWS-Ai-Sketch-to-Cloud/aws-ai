# 작업중

## 완료된 작업
- [x] README 기술 스택 백엔드 항목 `Python (FastAPI)`로 변경
- [x] A 산출물 3종 작성
- [x] `A_입력테스트셋_v1.md` 작성 (20개 입력 + 기대 JSON)
- [x] `A_JSON_스키마_v1.json` 작성
- [x] `A_프롬프트_v1.md` 작성
- [x] API/스키마 계약 문서 작성 (`API_스키마계약_v1.md`)
- [x] FastAPI 기본 API 구현 (`project/session/analyze/detail`)
- [x] Supabase PostgreSQL 연결 및 스키마 적용 확인
- [x] Bedrock 연동 구조 + 로컬 폴백(`BEDROCK_ENABLED=false`) 구현
- [x] 입력 20개 자동 평가 스크립트 작성 (`scripts/evaluate_inputs.py`)
- [x] 로컬 모드 기준 입력 20개 정확도 100% 확인
- [x] Terraform 생성 API 구현 (`POST /api/sessions/{id}/terraform`)
- [x] Terraform validate 자동 실행 및 결과 저장 구현
- [x] 비용 계산 API 구현 (`POST /api/sessions/{id}/cost`)
- [x] 세션 상세/목록 API 확장 (`GET /api/sessions/{id}`, `GET /api/projects/{id}/sessions`)
- [x] 실패 케이스 점검 스크립트 추가 (`scripts/check_failure_cases.py`)
- [x] pytest 단위 테스트 추가 및 통과
- [x] 통합 테스트 추가 (`tests/test_integration_flow.py`)
- [x] GitHub Actions 테스트 워크플로 추가 (`.github/workflows/backend-tests.yml`)
- [x] Alembic 초기 세팅 + 리비전 생성/적용 완료 (`01898398e88a`)
- [x] `.gitignore` 추가 및 `pyc` Git 추적 해제
- [x] 스모크 테스트 문서/스크립트 추가 (`docs/API_스모크테스트.md`, `scripts/smoke_api.ps1`)
- [x] API 샘플 응답 문서 추가 (`docs/API_샘플응답.md`)
- [x] DB 동기화 체크 스크립트 추가 (`scripts/check_db_sync.ps1`)
- [x] `서비스_파이프라인_초안.md`에 `owner_id` nullable 임시 운영 규칙 반영

## 다음 작업 (우선순위)
- [ ] 현재 변경사항 1차 커밋
- [ ] 팀원 환경에서 `scripts/check_db_sync.ps1` 실행으로 리비전 동기화 확인
- [ ] 스모크 테스트 스크립트(`scripts/smoke_api.ps1`) 팀원 공통 실행 확인
- [ ] Terraform CLI 설치 환경에서 `validationStatus=PASSED` 검증
- [ ] 비용 계산 로직 고도화 (정적 단가 -> 리전/타입별 테이블 기반)
- [ ] Bedrock 실제 모드(`BEDROCK_ENABLED=true`) 연결 검증
- [ ] 인증 연동 시 `projects.owner_id`를 `NOT NULL`로 전환하는 Alembic 마이그레이션 작성

## 작업 메모
- 현재 DB 초안 최종 구조 기준에서 예외는 `projects.owner_id` 임시 nullable 정책 1개
- `pyc`/`__pycache__`는 Git 추적 제외됨 (`.gitignore` 반영)
- AWS 비용 절약을 위해 기본 개발 모드는 `BEDROCK_ENABLED=false` 권장
