# 작업중

## 완료된 작업
- [x] README 기술 스택 백엔드 항목 `Python (FastAPI)`로 변경
- [x] A 산출물 3종 작성
- [x] `A_입력테스트셋_v1.md` 작성 (20개 입력 + 기대 JSON)
- [x] `A_JSON_스키마_v1.json` 작성
- [x] `A_프롬프트_v1.md` 작성
- [x] API/스키마 계약 문서 작성 (`API_스키마계약_v1.md`)
- [x] FastAPI 기본 API 구현 (`project/session/analyze/detail`)
- [x] Supabase PostgreSQL 연결 및 스키마 적용 확인
- [x] Bedrock 연동 구조 + 로컬 폴백(`BEDROCK_ENABLED=false`) 구현
- [x] 입력 20개 자동 평가 스크립트 작성 (정리 과정에서 제거됨)
- [x] 로컬 모드 기준 입력 40개(v1+v2) 정확도 100% 확인
- [x] Terraform 생성 API 구현 (`POST /api/sessions/{id}/terraform`)
- [x] Terraform validate 자동 실행 및 결과 저장 구현
- [x] 비용 계산 API 구현 (`POST /api/sessions/{id}/cost`)
- [x] 세션 상세/목록 API 확장 (`GET /api/sessions/{id}`, `GET /api/projects/{id}/sessions`)
- [x] 실패 케이스 점검 스크립트 추가 (정리 과정에서 제거됨)
- [x] Terraform 품질 점검 스크립트 추가 (정리 과정에서 제거됨)
- [x] pytest 단위 테스트 추가 및 통과
- [x] 통합 테스트 추가 (`tests/test_integration_flow.py`)
- [x] GitHub Actions 테스트 워크플로 추가 (`.github/workflows/backend-tests.yml`)
- [x] Alembic 초기 세팅 + 리비전 생성/적용 완료 (`01898398e88a`)
- [x] `.gitignore` 추가 및 `pyc` Git 추적 해제
- [x] 스모크 테스트 문서/스크립트 추가 (`backend/docs/API_스모크테스트.md`, `backend/scripts/smoke_api.ps1`)
- [x] API 샘플 응답 문서 추가 (`backend/docs/API_샘플응답.md`)
- [x] DB 동기화 체크 스크립트 추가 (`backend/scripts/check_db_sync.ps1`)
- [x] `서비스_파이프라인_초안.md`에 `owner_id` nullable 임시 운영 규칙 반영
<<<<<<< HEAD
- [x] Terraform `PASSED` 강제 검증 옵션 추가 (`scripts/smoke_api.ps1 -RequireTerraformPassed`)
- [x] Bedrock 실연동 단독 점검 스크립트 추가 (`scripts/verify_bedrock.py`)

## 다음 작업 (우선순위)
- [ ] 현재 변경사항 1차 커밋
- [ ] 팀원 환경에서 `alembic upgrade head` 및 `alembic current` 결과 동기화
- [ ] 스모크 테스트 스크립트(`scripts/smoke_api.ps1`) 팀원 공통 실행 확인
- [ ] Terraform CLI 설치 환경에서 `validationStatus=PASSED` 검증 (신규 옵션으로 체크 가능)
=======
- [x] `API_명세.md(v2)` 기준 백엔드 API 정합 반영
  - [x] 인증 API 추가 (`/api/auth/register`, `/api/auth/login`, `/api/auth/logout`, `/api/users/me`)
  - [x] 업로드 API 스텁 추가 (`/api/uploads/images`)
  - [x] 세션 상태 갱신 API 추가 (`PATCH /api/sessions/{sessionId}/status`)
  - [x] Terraform/Cost 응답에 `contractVersion: "v2"` 반영
  - [x] `users.login_id` 컬럼 및 인증 조회 기준을 명세대로 정렬
  - [x] v2 기준 스모크 테스트 문서/스크립트 동기화
  - [x] Terraform CLI 설치 및 실검증(`validationStatus=PASSED`) 완료
  - [x] Terraform 경로 자동탐지 보강(`PATH` 미반영 환경 포함)
  - [x] 프론트엔드 템플릿을 TailAdmin React(Vite)로 전환
  - [x] `/` 라우트에 API v2 콘솔 화면 구현(인증/세션/Terraform/비용)
  - [x] 템플릿 UI 단순화(사이드바/헤더 정리) 및 한글 패치 적용
  - [x] 프론트 라우트를 프로젝트 전용(`/`)으로 축소, 템플릿 페이지 제거

## 다음 작업 (우선순위)
- [ ] 현재 변경사항 1차 커밋
- [ ] 팀원 환경에서 `backend/scripts/check_db_sync.ps1` 실행으로 리비전 동기화 확인
- [ ] 스모크 테스트 스크립트(`backend/scripts/smoke_api.ps1`) 팀원 공통 실행 확인
- [x] Terraform CLI 설치 환경에서 pass ratio 검증 (40/40, 100%)
>>>>>>> fd3fb12622ddb71bf152b86875fe647ed0321885
- [ ] 비용 계산 로직 고도화 (정적 단가 -> 리전/타입별 테이블 기반)
- [ ] Bedrock 실제 모드(`BEDROCK_ENABLED=true`) 연결 검증 (`scripts/verify_bedrock.py`로 사전 점검 가능)
- [ ] 인증 연동 시 `projects.owner_id`를 `NOT NULL`로 전환하는 Alembic 마이그레이션 작성
- [ ] 인증 토큰을 개발용 임시 포맷(`Bearer uid:{userId}`)에서 JWT 방식으로 교체
- [ ] 업로드 API를 실제 스토리지(Supabase Storage 또는 S3 presigned URL)로 교체

## 작업 메모
- 현재 DB 초안 최종 구조 기준에서 예외는 `projects.owner_id` 임시 nullable 정책 1개
- `pyc`/`__pycache__`는 Git 추적 제외됨 (`.gitignore` 반영)
- AWS 비용 절약을 위해 기본 개발 모드는 `BEDROCK_ENABLED=false` 권장

## 디자인 템플릿 url
https://github.com/TailAdmin/tailadmin-free-tailwind-dashboard-template

## 기능 목록
1. 회원가입
2. 로그인
3. 로그아웃
4. 내 정보 조회
5. 프로젝트 생성
6. 프로젝트 목록 조회
7. 프로젝트 선택
8. 세션 생성
9. 프로젝트별 세션 목록 조회
10. 텍스트 요구사항 입력
11. 스케치 이미지 URL 입력
12. 입력값 유효성 검사
13. 자동 실행(분석 -> Terraform -> 비용 계산)
14. 아키텍처 결과 표시
15. Terraform 코드 표시
16. Terraform 검증 결과 표시
17. 비용 총액 표시
18. 비용 상세 항목 표시
19. 세션 상태 표시
20. 세션 상태 수동 갱신(PATCH)
21. 에러 메시지 표시
22. 실행 로그 표시
23. 한글 UI 문구 통일
24. 모바일 반응형 대응
25. 전체 사용자 흐름 E2E 테스트(가입 -> 생성 -> 결과 확인)
